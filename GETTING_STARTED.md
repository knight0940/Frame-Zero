# Frame Zero - 快速开始指南

## 项目初始化完成 ✅

项目基础架构已经搭建完成！现在你可以开始开发了。

## 📁 项目结构

```
frame-zero/
├── backend/              # NestJS 后端
│   ├── src/
│   │   ├── main.ts      # 应用入口
│   │   └── app.module.ts # 根模块
│   ├── prisma/
│   │   ├── schema.prisma # 数据库模型
│   │   └── seed.ts      # 种子数据
│   ├── package.json
│   └── .env             # 环境变量配置
│
├── frontend/             # Next.js 前端
│   ├── src/
│   │   ├── app/         # App Router 页面
│   │   ├── components/  # React 组件
│   │   ├── lib/         # 工具函数
│   │   └── styles/      # 全局样式
│   ├── package.json
│   └── .env.local       # 环境变量配置
│
├── shared/               # 共享代码
│   └── types/           # TypeScript 类型定义
│
├── docs/                 # 项目文档
│   ├── 需求文档.md
│   ├── API接口文档.md
│   ├── database.md
│   └── deployment.md
│
└── docker-compose.yml    # Docker 服务配置
```

## 🚀 快速开始

### 1. 启动数据库服务

```bash
docker-compose up -d
```

验证服务状态：
```bash
docker-compose ps
```

### 2. 后端设置

```bash
cd backend

# 安装依赖
npm install

# 初始化数据库
npx prisma generate
npx prisma db push

# （可选）运行种子数据
npx prisma db seed

# 启动开发服务器
npm run start:dev
```

后端将运行在: http://localhost:3001

### 3. 前端设置

打开新终端窗口：

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端将运行在: http://localhost:3000

## 📝 下一步开发

### 后端开发

根据 [API接口文档](../docs/API接口文档.md)，你需要创建以下模块：

1. **认证模块** (`src/modules/auth/`)
   - 注册/登录
   - JWT 策略
   - 权限守卫

2. **用户模块** (`src/modules/user/`)
   - 用户 CRUD
   - 个人资料管理

3. **帖子模块** (`src/modules/post/`)
   - 帖子 CRUD
   - 评论功能
   - 点赞功能

4. **打卡模块** (`src/modules/check-in/`)
   - 每日打卡
   - 排行榜

5. **通知模块** (`src/modules/notification/`)
   - WebSocket 通知
   - 通知中心

### 前端开发

根据 [需求文档](../docs/需求文档.md)，你需要创建以下组件：

1. **布局组件** (`src/components/layout/`)
   - VSCodeLayout - 主布局
   - ActivityBar - 活动栏
   - Sidebar - 侧边栏
   - StatusBar - 状态栏

2. **基础 UI 组件** (`src/components/ui/`)
   - Button, Input, Modal 等
   - 基于 shadcn/ui

3. **页面组件** (`src/app/`)
   - 登录/注册页面
   - 首页
   - 板块页面

## 🗄️ 数据库管理

### 查看数据库

```bash
cd backend
npx prisma studio
```

访问 http://localhost:5555 查看数据库内容

### 重置数据库

```bash
cd backend
npx prisma migrate reset
```

### 添加新模型

1. 编辑 `backend/prisma/schema.prisma`
2. 运行 `npx prisma db push`

## 🧪 测试账号

种子数据会创建以下测试账号：

**创始人账号:**
- 邮箱: `admin@framezero.com`
- 密码: 需要在运行种子数据前设置

**测试用户:**
- 邮箱: `user@framezero.com`
- 密码: 需要在运行种子数据前设置

**注意**: 目前密码使用的是占位符，你需要先用 bcrypt 生成实际密码哈希值。

## 📚 相关文档

- [需求文档.md](../docs/需求文档.md) - 完整的产品需求
- [API接口文档.md](../docs/API接口文档.md) - API 接口定义
- [database.md](../docs/database.md) - 数据库设计详情
- [deployment.md](../docs/deployment.md) - 部署和运维指南

## 🐛 常见问题

### 端口冲突

如果 3000 或 3001 端口被占用：

```bash
# 查看占用端口的进程
lsof -i :3000
lsof -i :3001

# 终止进程
kill -9 <PID>
```

### Docker 容器问题

```bash
# 查看容器日志
docker-compose logs postgres
docker-compose logs redis

# 重启容器
docker-compose restart
```

### 数据库连接失败

确保 PostgreSQL 容器正在运行：

```bash
docker-compose ps
```

如果未运行，启动它：

```bash
docker-compose up -d
```

## 🎯 开发建议

1. **按照文档顺序开发**: 先实现认证，再实现其他功能
2. **保持文档同步**: 代码变更时记得更新文档
3. **使用 TypeScript**: 严格类型检查
4. **代码规范**: 使用 ESLint 和 Prettier
5. **Git 提交**: 写清晰的 commit message

## 📞 需要帮助？

- 查看 [文档](../docs/)
- 提交 [Issue](https://github.com/yourusername/frame-zero/issues)
- 联系: support@framezero.com

---

**祝开发顺利！🎉**
