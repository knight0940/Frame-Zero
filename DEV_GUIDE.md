# Frame Zero - 开发环境使用指南

## 🚀 快速开始

### 方式一：使用开发脚本（推荐）

我们提供了自动化脚本来管理开发环境：

#### 1. 启动开发环境

```bash
./dev-start.sh
```

这个脚本会自动：
- ✅ 启动 Docker 数据库服务（PostgreSQL + Redis）
- ✅ 检查并安装依赖（如需要）
- ✅ 初始化数据库
- ✅ 启动后端服务（http://localhost:3001）
- ✅ 启动前端服务（http://localhost:3000）

#### 2. 查看状态

```bash
./dev-status.sh
```

查看所有服务的运行状态和端口占用情况。

#### 3. 停止开发环境

```bash
./dev-stop.sh
```

停止前端和后端服务（可选择是否停止数据库）。

---

### 方式二：手动启动

如果你想手动控制每个步骤：

#### 1. 启动数据库

```bash
docker-compose up -d postgres redis
```

#### 2. 启动后端

```bash
cd backend

# 首次运行需要安装依赖
npm install

# 初始化数据库
npx prisma generate
npx prisma db push

# 启动开发服务器
npm run start:dev
```

#### 3. 启动前端（新终端窗口）

```bash
cd frontend

# 首次运行需要安装依赖
npm install

# 启动开发服务器
npm run dev
```

---

## 📱 访问应用

启动成功后，访问以下地址：

- **前端应用**: http://localhost:3000
- **后端 API**: http://localhost:3001
- **数据库**: localhost:5432
- **Redis**: localhost:6379

---

## 📝 查看日志

### 使用开发脚本

日志文件位于 `logs/` 目录：

```bash
# 查看后端日志
tail -f logs/backend.log

# 查看前端日志
tail -f logs/frontend.log
```

### 手动启动时

日志会直接输出到终端。

---

## 🛠️ 常用命令

### 后端

```bash
cd backend

# 安装依赖
npm install

# 启动开发服务器
npm run start:dev

# 构建生产版本
npm run build

# 启动生产服务器
npm run start:prod

# 运行测试
npm test

# 代码检查
npm run lint

# Prisma 操作
npx prisma studio        # 打开数据库管理界面
npx prisma generate      # 生成 Prisma Client
npx prisma db push       # 推送数据库结构
npx prisma migrate dev   # 运行数据库迁移
```

### 前端

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm run start

# 代码检查
npm run lint

# 代码格式化
npm run format
```

---

## 🔧 故障排查

### 端口被占用

如果端口被占用，查看并终止进程：

```bash
# 查看占用端口的进程
lsof -i :3000  # 前端
lsof -i :3001  # 后端

# 终止进程
kill -9 <PID>
```

### Docker 问题

```bash
# 查看 Docker 日志
docker-compose logs postgres
docker-compose logs redis

# 重启 Docker 服务
docker-compose restart

# 完全删除并重新创建
docker-compose down -v
docker-compose up -d
```

### 数据库连接失败

确保数据库容器正在运行：

```bash
docker-compose ps
```

如果未运行，启动它：

```bash
docker-compose up -d
```

### 依赖问题

如果遇到依赖问题，尝试重新安装：

```bash
# 清理并重新安装
rm -rf node_modules package-lock.json
npm install
```

---

## 📂 项目结构

```
frame-zero/
├── backend/              # NestJS 后端
│   ├── src/              # 源代码
│   ├── prisma/           # 数据库
│   └── logs/             # 日志文件
│
├── frontend/             # Next.js 前端
│   ├── src/              # 源代码
│   ├── public/           # 静态资源
│   └── logs/             # 日志文件
│
├── shared/               # 共享代码
│   └── types/           # TypeScript 类型
│
├── docs/                 # 项目文档
│
├── logs/                 # 开发日志
│   ├── backend.log       # 后端日志
│   └── frontend.log      # 前端日志
│
├── dev-start.sh         # 启动脚本
├── dev-stop.sh          # 停止脚本
├── dev-status.sh        # 状态检查脚本
└── docker-compose.yml   # Docker 配置
```

---

## 💡 开发建议

1. **使用开发脚本**: 使用 `./dev-start.sh` 和 `./dev-stop.sh` 快速启动/停止环境
2. **定期检查状态**: 使用 `./dev-status.sh` 查看服务状态
3. **查看日志**: 出现问题时先查看日志文件
4. **保持更新**: 定期更新依赖包
5. **代码规范**: 提交代码前运行 lint 检查

---

## 🐛 报告问题

如果遇到问题：

1. 查看日志文件（logs/backend.log 或 logs/frontend.log）
2. 使用 `./dev-status.sh` 检查服务状态
3. 查看 [故障排查](#-故障排查) 部分
4. 提交 Issue: https://github.com/yourusername/frame-zero/issues

---

**祝开发顺利！🎉**
